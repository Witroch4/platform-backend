#!/usr/bin/env tsx
/**
 * ❌ SCRIPT DEPRECIADO - SERÁ REMOVIDO
 *
 * MOTIVO: TURBO Mode é funcionalidade core do sistema, não precisa de feature flag.
 * O controle de acesso é feito diretamente via UserFeatureFlagOverride sem flag global.
 *
 * SOLUÇÃO: Controle direto de acesso por usuário sem intermediação de flag global.
 */

console.log("❌ Este script foi depreciado.");
console.log("🎯 TURBO Mode é funcionalidade core do sistema.");
console.log("💡 Use controle direto de acesso por usuário.");

process.exit(0);
