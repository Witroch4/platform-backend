# ======================================================
# ARQUIVO .ENV CORRIGIDO E ORGANIZADO
# ======================================================

# --- URLs de Acesso ---
# Domínio da interface do Editor do n8n
N8N_HOST=autofluxofila.witdev.com.br
# ADICIONADO: Domínio exclusivo para os Webhooks
N8N_WEBHOOK_HOST=autofluxofilaapi.witdev.com.br
# URL completa do Editor (DEVE corresponder a N8N_HOST)
N8N_EDITOR_BASE_URL=https://autofluxofila.witdev.com.br/
# URL completa dos Webhooks (DEVE corresponder a N8N_WEBHOOK_HOST)
WEBHOOK_URL=https://2357witflowauto.witdev.com.br/
# REMOVIDO: N8N_PROTOCOL=https (Não é mais necessário)

# --- Conexão com Banco de Dados (PostgreSQL) ---
DB_TYPE=postgresdb
DB_POSTGRESDB_HOST=postgres
DB_POSTGRESDB_PORT=5432
DB_POSTGRESDB_DATABASE=n8n_queue
DB_POSTGRESDB_USER=postgres
DB_POSTGRESDB_PASSWORD=WITrdN6835yt8roch4WITrdNwit2357ytwqw466tgroch4696WIT

# --- Conexão com Fila (Redis) ---
QUEUE_BULL_REDIS_HOST=redis
QUEUE_BULL_REDIS_PORT=6379
QUEUE_BULL_REDIS_DB=2

# --- Chave de Criptografia ---
N8N_ENCRYPTION_KEY=5656fe8d57de4ed3b6da2b63b64b25b9

# --- Configurações Gerais do Ambiente ---
NODE_ENV=production
GENERIC_TIMEZONE=America/Sao_Paulo
TZ=America/Sao_Paulo

# --- Configurações de Execução do n8n ---
EXECUTIONS_MODE=queue
EXECUTIONS_DATA_PRUNE=true
EXECUTIONS_DATA_MAX_AGE=336
NODE_FUNCTION_ALLOW_BUILTIN=*
NODE_FUNCTION_ALLOW_EXTERNAL=moment,lodash,axios,axios-ntlm
N8N_COMMUNITY_PACKAGES_ENABLED=true
N8N_REINSTALL_MISSING_PACKAGES=true